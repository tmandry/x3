swift_version = '4.0'
platform :osx, '10.10'

target 'x3' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for x3
  pod 'Swindler', path: './Swindler'

  target 'x3Tests' do
    inherit! :search_paths
    pod 'Nimble', '~> 7.3.0'
    pod 'Quick', '~> 1.3.0'
  end

end
